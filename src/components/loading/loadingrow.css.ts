import { style } from "@vanilla-extract/css";

export const loadingRowCss = style({
    display: "flex",
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "center",
    padding: "16px",
});
